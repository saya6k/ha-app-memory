# Agent scaffolding (AGENTS.md, .agents/, .claude/, .gemini/, and the
# CLAUDE.md / GEMINI.md symlinks) is per-developer tooling, not repo content.
# It stays on disk locally and is never tracked.
AGENTS.md
CLAUDE.md
GEMINI.md
.agents/
.claude/
.gemini/
# AI working state — design notes and plan, not project docs
SPEC.md
tasks/
# Node
node_modules/
dist/
# Local add-on data (models, sqlite) — never committed
data/
*.gguf
*.sqlite
*.sqlite-*
.DS_Store
